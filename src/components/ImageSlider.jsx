import PropTypes from "prop-types";

ImageSlider.propTypes = {
  imageArr: PropTypes.array.isRequired,
  thumbnailArr: PropTypes.array.isRequired,
};
export default function ImageSlider({ imageArr, thumbnailArr }) {}
